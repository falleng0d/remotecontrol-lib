name: remotecontrol_lib
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.0 <4.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.10.0
  test: ^1.16.0
  win32: ^4.1.3
dependencies:
  intl: ^0.17.0
  flutter:
    sdk: flutter
  # fluent_ui 4.5.1 (was 4.4.2) (4.6.2 available)
  fluent_ui: ^4.4.2
  grpc:
  protobuf:
  xml: ^6.1.0
  get: ^4.6.5

flutter:
  uses-material-design: true
